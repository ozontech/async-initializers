name: Publish Gradle Plugin to Plugin Portal

on:
  push:
    tags:
      - 'plugin-*'

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Validate version matches versions.properties
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#plugin-}"
          EXPECTED_VERSION=$(grep '^versionName=' plugin/versions.properties | sed 's/^versionName=//')
          if [ "$TAG_VERSION" != "$EXPECTED_VERSION" ]; then
            echo "::error::Tag version ($TAG_VERSION) does not match versions.properties ($EXPECTED_VERSION)"
            exit 1
          fi
          echo "Version validated: $EXPECTED_VERSION"

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Publish
        run: ./gradlew publishPlugins
        env:
          GRADLE_PUBLISH_KEY: ${{ secrets.GRADLE_PUBLISH_KEY }}
          GRADLE_PUBLISH_SECRET: ${{ secrets.GRADLE_PUBLISH_SECRET }}


      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          generate_release_notes: true
